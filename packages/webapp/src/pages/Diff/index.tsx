import { Container } from '@material-ui/core';
import React from 'react';
import RenderAll from '../../components/DiffView/renderAll';

const Home: React.FC = () => {
  return (
    <Container>
      <RenderAll />
    </Container>
  );
};

export default Home;
