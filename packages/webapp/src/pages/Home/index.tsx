import React from 'react';
import DefaultPageLayout from '../../components/DefaultPageLayout';

const Home: React.FC = () => {
  return (
    <div>
      <DefaultPageLayout />
    </div>
  );
};

export default Home;
